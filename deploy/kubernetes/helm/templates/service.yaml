apiVersion: v1
kind: Service
metadata:
  name: {{ include "coolify-mcp-server.fullname" . }}
  labels:
    {{- include "coolify-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coolify-mcp-server.selectorLabels" . | nindent 4 }}
